-> get simple animate working - use move, get one player moving around smoothly (X)
-> get a point on a graph moving around and saved to a file using matplotlib
-> annotate an event/ add multiple players in simple 
-> multiple players in matplotlib 
    data set up like 
-> make it look like a field in matplotlib (X)
-> 
-> 

how to deal with incomplete information - "fog of war", making players more or less saturated depending on confidence of location, looking at players who leave the screen, do we show them
do we do some inference - visual feedback of limits of camera


how do we deal with confidence values? Should we just have a simpler check for "interpolated value", and change
the color accordingly?

let's test some interpolation
